export function PitchActions({ disabled, onSave, onDuplicate, onDelete, onConvert }: { disabled?: boolean; onSave: () => void; onDuplicate: () => void; onDelete: () => void; onConvert: () => void }) {
  return <div className="grid gap-2 sm:grid-cols-2"><button disabled={disabled} className="evidence-button disabled:opacity-50" onClick={onSave}>Save Pitch</button><button disabled={disabled} className="stamp border-brass text-brass disabled:opacity-50" onClick={onDuplicate}>Duplicate Draft</button><button disabled={disabled} className="stamp border-crimson text-crimson disabled:opacity-50" onClick={onDelete}>Delete Pitch</button><button disabled={disabled} className="stamp border-teal text-teal disabled:opacity-50" onClick={onConvert}>Convert to Storyline Draft</button></div>;
}
